(function ($) {
    var dateHelper = {
        getDate:function() {
           
        }
    };

    if(!window.commonTools)
    {
        window.commonTools={};
    }
    window.commonTools.dateHelper = dateHelper;
}(jQuery));
